Student Management REST API

This project is a simple REST API developed using Node.js and Express.js for managing student records.
It demonstrates CRUD (Create, Read, Update, Delete) operations, REST API concepts, routing, middleware, and error handling.
